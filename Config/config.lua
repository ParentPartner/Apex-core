-- Config/config.lua
Config = {}

-- Default starting position for new players or if no saved position is found
Config.StartingPosition = {
    x = 0.0,
    y = 0.0,
    z = 72.0
}

-- Add other configuration settings here as needed
